#ifndef STRGICMP_H
#define STRGICMP_H

#include "types.h"
#include "ptypes.h"

extern at_bool strgicmp (const char *s1, const char *s2);
extern at_bool strgnicmp (const char *s1, const char *s2, long len);

#endif /* not STRGICMP_H */
